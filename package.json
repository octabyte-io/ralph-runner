{
  "name": "ralph-runner",
  "version": "0.1.0",
  "type": "module",
  "description": "Multi-agent RALPH: dependency-aware parallel issue runner for Claude Code",
  "repository": "github:octabyte-io/ralph-runner",
  "license": "MIT",
  "bin": {
    "ralph": "bin/ralph.mjs"
  },
  "files": [
    "bin/",
    "dist/",
    "src/",
    "prompt-template.md"
  ],
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "prepare": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "node --test 'test/*.test.ts'"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}
